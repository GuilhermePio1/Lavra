# Normaliza fim de linha: repositório guarda LF; cada SO faz checkout no seu padrão
* text=auto

# Scripts PowerShell funcionam melhor com CRLF no Windows
*.ps1 text eol=crlf

# Binários nunca são normalizados
*.mp3 binary
*.wav binary
*.png binary
*.jpg binary
